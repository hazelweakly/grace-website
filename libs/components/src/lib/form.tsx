import { Root, Field, Label, Message, Control, Submit } from '@radix-ui/react-form';

export const Form = () => (
    <Root>
        <Field>
            <Label></Label>
            <Message></Message>
            <Control></Control>
        </Field>
        <Submit></Submit>
    </Root>
);

export default Form;
